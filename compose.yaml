services:
  vpn:
    image: qmcgaw/gluetun:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file: vpn.env
    environment:
      VPN_SERVICE_PROVIDER: custom
      VPN_TYPE: openvpn
      OPENVPN_CUSTOM_CONFIG: /gluetun/custom.conf
      FIREWALL_INPUT_PORTS: 8081
      DNS_UPSTREAM_RESOLVER_TYPE: plain
      DNS_UPSTREAM_PLAIN_ADDRESSES: 10.4.20.21:53
      DNS_REBINDING_PROTECTION_EXEMPT_HOSTNAMES: mess.iiit.ac.in
      HEALTH_TARGET_ADDRESSES: mess.iiit.ac.in:443
      HEALTH_ICMP_TARGET_IPS: 10.4.20.21
      BLOCK_MALICIOUS: "off"
    volumes:
      - ./iiith.ovpn:/gluetun/custom.conf:ro
    networks:
      glance:
        aliases:
          - iiith-mess

  proxy:
    image: caddy:2-alpine
    restart: unless-stopped
    network_mode: service:vpn
    depends_on:
      vpn:
        condition: service_healthy
    env_file: mess.env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro

networks:
  glance:
    external: true
    name: ${GLANCE_NETWORK:-glance_default}
